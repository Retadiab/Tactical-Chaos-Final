package ChampionClasses;

import Champion.Champion;
import Damage.DamageCalculatorFactory;

public class Dragon {
    public Champion ActiveAbilityFromClasses(Champion Same , int numberOfChampions){
        if(numberOfChampions >=2){
//            Same.setAbilityArmor(true);
            //just edit the attributes in champio
        }
        return Same;
}}
